import 'package:flutter/material.dart';

class VendorMyShop extends StatelessWidget {
  const VendorMyShop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Shop')),
      body: const Center(child: Text('My Shop Screen Placeholder')),
    );
  }
}
